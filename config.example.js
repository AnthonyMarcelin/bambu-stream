// Copy this file to config.js and fill in your printer details.
// Screen -> Settings -> LAN Only shows the IP and access code.
export const config = {
  printerIp: "192.168.1.x",
  accessCode: "xxxxxxxx",
  wsPort: 7781, // local WebSocket port the OBS overlay connects to
};
